class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        nz = -1

        for i in range(len(nums)):
            if nums[i] != 0:
                nz += 1
                temp = nums[nz]
                nums[nz] = nums[i]
                nums[i] = temp
